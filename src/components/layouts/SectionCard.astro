---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<section
  class={`w-full bg-card rounded border-2 border-border p-6 group ${className}`}
>
  <slot />
</section>

