---
import { useTranslations } from "../i18n/useTranslations";

const locale = Astro.currentLocale ?? "en";
const t = useTranslations(locale);
---

<nav
  class="text-foreground items-center bg-background border-primary w-full h-16 border-t-2 bottom-0 px-6 fixed flex justify-center"
>
  <p class="text-primary animate-pulse">
    {t("webringNav")["loading"]}
  </p>
</nav>
